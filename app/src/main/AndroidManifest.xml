<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Runtime permissions used by Backup & Restore -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- Android 12 and below -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <application
        android:name="com.faroquetech.theme.RootRealmGlobalTheme"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".DeviceInfoActivity" android:exported="false" />
        <activity android:name=".AdbFastbootActivity" android:exported="false" />
        <activity android:name=".BackupRestoreActivity" android:exported="false" />
        <activity android:name=".DebloaterActivity" android:exported="false" />
        <activity android:name=".RootManagerActivity" android:exported="false" />
        <activity android:name=".DisplayManagerActivity" android:exported="false" />
        <activity android:name=".CpuManagerActivity" android:exported="false" />
        <activity android:name=".GpuManagerActivity" android:exported="false" />
        <activity android:name=".ThermalManagerActivity" android:exported="false" />
        <activity android:name=".BatteryManagerActivity" android:exported="false" />

        <activity android:name=".FlashModuleActivity" android:exported="false" />
        <activity android:name=".OtaExtractorActivity" android:exported="false" />

        <activity
            android:name="com.faroquetech.theme.ThemeManagerActivity"
            android:exported="false" />

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <activity android:name=".ShizukuManagerActivity" android:exported="false" />

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
